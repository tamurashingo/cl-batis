(in-package :cl-user)
(defpackage batis-test
  (:use :cl
        :batis
        :prove))
(in-package :batis-test)

;; NOTE: To run this test file, execute `(asdf:test-system :batis)' in your Lisp.

(plan nil)

;; blah blah blah.

(finalize)
